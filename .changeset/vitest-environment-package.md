---
'vitest-environment-happy-dom-extended': minor
---

Add a Vitest environment that installs the same Happy DOM Canvas, Worker, and Web API extensions as the Jest package. Configure it with `environment: 'happy-dom-extended'`.
